"""
Agent module — classify ticket → search docs → respond (LangGraph, tool calling).

Weeks 3–4: Multi-agent flows and tool calling with Claude.
Run: uv run python -m modules.agent.main
Install deps: uv sync --extra agent
"""


def main() -> None:
    print("Agent module: classify ticket → search docs → respond (placeholder).")
    # TODO: LangGraph graph, tools (search_docs, etc.), Claude with tool use


if __name__ == "__main__":
    main()
